# For more information, see the Configuration Guide:
# https://www.librechat.ai/docs/configuration/librechat_yaml

version: 1.2.1
cache: true

interface:
  customWelcome: 'Welcome to LibreChat! Enjoy your experience.'
  fileSearch: true
  privacyPolicy:
    externalUrl: 'https://librechat.ai/privacy-policy'
    openNewTab: true
  termsOfService:
    externalUrl: 'https://librechat.ai/tos'
    openNewTab: true
    modalAcceptance: true
    modalTitle: 'Terms of Service for LibreChat'
    modalContent: |
      See https://librechat.ai/tos for current terms.

  endpointsMenu: true
  modelSelect: true
  parameters: true
  sidePanel: true
  presets: true
  prompts: true
  bookmarks: true
  multiConvo: true
  agents: true
  peoplePicker:
    users: true
    groups: true
    roles: true
  marketplace:
    use: false
  fileCitations: true
  # temporaryChatRetention: 720

registration:
  # Keep social list lean; you can re-enable later
  socialLogins: ['google']

# ---- Rate limits / balances left as defaults ----
# speech: ...
# rateLimits: ...
# balance: ...
# transactions: ...

# ===== Actions: pre-allow domains we’ll use for tools =====
actions:
  allowedDomains:
    - 'api.pushover.net'          # Pushover
    - 'plex.tv'                   # Plex account & discovery
    - 'app.plex.tv'
    - 'plex.direct'
    - 'notion.so'                 # Notion Content API
    - 'api.notion.com'
    - 'api.openweathermap.org'    # OpenWeather
    - 'smithery.ai'               # Smithery
    - 'librechat.ai'
    - 'google.com'

# ===== Endpoints =====
endpoints:

  custom:

    # ---------- Google (Gemini) via Google AI Studio ----------
    - name: 'Google'
      apiKey: '${GOOGLE_KEY}'
      baseURL: 'https://generativelanguage.googleapis.com'
      models:
        default:
          [
            'google/gemini-2.5-flash',
            'google/gemini-2.0-flash-001',
            'google/gemini-2.5-pro',
            'google/gemini-2.5-flash-lite',
            'google/gemma-3-12b-it',
            'google/gemini-2.5-flash-image-preview',
            'google/gemma-3-27b-it:free'
          ]
        fetch: false
      titleConvo: true
      titleModel: 'google/gemini-2.5-flash'
      modelDisplayLabel: 'Google (Gemini)'

    # ---------- OpenAI ----------
    - name: 'OpenAI'
      apiKey: '${OPENAI_API_KEY}'
      baseURL: 'https://api.openai.com/v1'
      models:
        default:
          [
            'gpt-5',            # “GPT5”
            'gpt-5-nano',      # “GPT5 nano”
            'gpt-5-mini',      # “GPT5 mini”
            'gpt-4.1',
            'openai/gpt-oss-120b',
            'openai/gpt-oss-20b'
          ]
        fetch: false
      titleConvo: true
      titleModel: 'gpt-4.1'
      modelDisplayLabel: 'OpenAI'

    # ---------- OpenRouter (curated; NO Grok/xAI/Google) ----------
    - name: 'OpenRouter'
      apiKey: '${OPENROUTER_API_KEY}'
      baseURL: 'https://openrouter.ai/api/v1'
      headers:
        x-librechat-body-parentmessageid: '{{LIBRECHAT_BODY_PARENTMESSAGEID}}'
      models:
        default:
          [
            'openai/gpt-4o-mini',
            'meta-llama/llama-3.1-70b-instruct',
            'meta-llama/llama-3.1-8b-instruct',
            'mistralai/mistral-large-latest',
            'mistralai/mixtral-8x7b-instruct',
            'nousresearch/hermes-3-llama-3.1-70b',
            'qwen/qwen2.5-72b-instruct',
            'perplexity/llama-3.1-sonar-small-128k-online'
          ]
        fetch: false
      dropParams: ['stop']
      titleConvo: true
      titleModel: 'meta-llama/llama-3.1-70b-instruct'
      modelDisplayLabel: 'OpenRouter'
